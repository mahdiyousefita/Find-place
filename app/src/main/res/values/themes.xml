<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FindPlace" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="buyTicketMenuTheme">
        <item name="android:textColor">@color/myBlue</item>
        <item name="android:popupMenuStyle">@style/popupBuyTicketBackground</item>
        <item name="android:fontFamily">@font/basic</item>
        <item name="android:popupElevation">@dimen/standardMenuElevation</item>
    </style>

    <style name="popupBuyTicketBackground">
        <item name="android:popupBackground">@drawable/shape_popup_menu_background</item>
    </style>
</resources>